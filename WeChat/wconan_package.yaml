# auto generated by wconan2
default:
  name: wcdb-cpp
  version: 1.0.4
  build_types: static, shared
  build_configs: release

  dependencies:
    sqlcipher: 1.0.1-SNAPSHOT

  dependency_options:
    shared:
      default: false

  download:
    dir: ./wconan_libs
    recursive: true

  build:
    system: cmake
    arguments:

  outputs:
    headers: [ .wconan2/include ]


# android:


ios:
  build: &xcode_build
    system: xcodebuild
    project: ../apple/WCDB.xcodeproj
    targets: [ WCDBCpp ]
  outputs:


macos:
  build:
    <<: *xcode_build
    arguments: "LLVM_LTO=NO"
  outputs:


watchos:
  build:
    <<: *xcode_build
  outputs:


# windows:

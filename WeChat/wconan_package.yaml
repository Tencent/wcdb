# auto generated by wconan2
default:
  name: wcdb-cpp
  version: 1.0.0
  build_types: static, shared
  build_configs: release, debug

  dependencies:
    sqlcipher: 1.0.0-SNAPSHOT

  build:
    system: cmake
    path: CMakeLists.txt
    arguments: 

  outputs:
    headers: [ .wconan2/output/include ]

# android:

ios:
  build: &xcode_build
    system: xcodebuild
    project: ../apple/WCDB.xcodeproj
    targets: [ WCDBCpp ]
    arguments:

macos:
  build:
    <<: *xcode_build

# windows:

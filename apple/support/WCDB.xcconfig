#include "Version.xcconfig"
#include "../../sqlcipher/macosx/SQLite.xcconfig"

// Linking
MACH_O_TYPE = mh_dylib
PRODUCT_NAME = WCDB

// Packaging
INFOPLIST_FILE = $(SRCROOT)/support/Info.plist
MODULEMAP_FILE = $(SRCROOT)/support/WCDB.modulemap
PRODUCT_BUNDLE_IDENTIFIER = com.Tencent.WCDB

// Preprocessing
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) $(WCDB_VERSION_DEFINITIONS) $(SQLITE_PREPROCESSOR_DEFINITIONS) ZLIB_CONST=1 WCDB_BUILTIN_SQLCIPHER=1
